/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "glorious-raccoon-188.convex.cloud",
      },
    ],
  },
};

export default nextConfig;
